[package]
name = "wirehop-vt"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Off-screen virtual-terminal grid and render primitives (alacritty-backed), shared by WireHop session resume and WireTap session capture."
repository = "https://github.com/Keikai-Inc/wirehop"
readme = false

[lib]
name = "hop_vt"

[dependencies]
alacritty_terminal = { version = "0.26", default-features = false }

[dev-dependencies]
