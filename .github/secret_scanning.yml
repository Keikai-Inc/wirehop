# Secret-scanning configuration.
#
# Google OAuth credentials are NOT in this repository: they are injected at
# build time from the release environment and stored obfuscated in the binary
# (see crates/hop-cli/build.rs). Any credential-shaped string found here is a
# real finding, so there are deliberately no path exemptions for them.
#
# The only reviewed exception is a synthetic `sk-ant-oat01-...` fixture in
# crates/hop-cli/src/oauth.rs that exercises an output parser.
paths-ignore: []
