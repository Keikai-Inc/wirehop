# Suppress GitHub secret-scanning alerts for reviewed, intentional matches.
# See SECURITY.md "Known, intentional non-vulnerabilities" for the rationale
# (Google Desktop-app OAuth client — embedded by design; synthetic test
# fixtures). Any credential-shaped string OUTSIDE these paths is a real alert.
paths-ignore:
  - "crates/hop-cli/src/oauth.rs"
